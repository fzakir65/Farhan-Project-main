"""Task 4 — optimizer (NOT YET IMPLEMENTED; skeleton only).

Rebalance after safety cuts so the formula still sums to 100 % and stays inside the
Carles layer ranges; redistribute removed mass within the same layer first, then across
layers, never re-raising a material above any cap the safety engine applied.
"""
from __future__ import annotations


def optimize(*args, **kwargs):
    raise NotImplementedError("Task 4 — implement after Task 3 is confirmed (see CLAUDE.md)")
