"""Task 5 — match structured preferences to a perfume in dataset1 (NOT YET IMPLEMENTED).

The prompt MUST include the catalogue and instruct: "Only recommend from this list".
"""
from __future__ import annotations


def match(*args, **kwargs):
    raise NotImplementedError("Task 5 (see CLAUDE.md)")
