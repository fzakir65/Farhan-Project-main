# PREPROCESSING REPORT — Perfumery ML project

Generated by `preprocessing/run_preprocessing.py` (seed 42). Implements plan §F; assumes Tier-0 scope (checkpoint L.3 default), so no constraint tables are built (F.6 skipped).

## Executive summary

- **Note bridge (L.4)**: 170/271 rule-space notes now map into the perfume space (124 auto-accepted, 46 pending review); unified note ids issued for all 883 catalog notes.
- **Recipes**: 196 duplicate rows removed, 102 accords renormalised to 100%, zero remaining sum or min/max violations.
- **Blends**: 2766 (primary, secondary) pair recipes materialised with placeholder weights 0.65/0.35.
- **Coverage gap**: 13 primary target accords lack any rule recipe — flagged for a decision before Stage-3/4 training.
- **Features**: note table (883 × 13 features, family-mean imputation), training table (10807 × 125), feature spec for the Stage-1 encoder.
- **Splits**: stratified 80/10/10 with all-classes-present guarantee; class-weight table for the Stage-2 loss.

## Items needing human review / sign-off

1. Note bridge: 122 medium-confidence pairs across both directions need human review (filter `needs_review == True` in the 02_note_bridge_* tables). Unmatched notes stay space-local — decide per plan F.1 whether to map manually or tag outputs as rule-derived vs real-derived.
2. Unmapped accord tokens: confirm the 10 proposals in 03_unmapped_accord_token_resolution.csv (auto-threshold 0.8).
3. 13 primary target accords (503 training examples) have no rule recipe — Stages 3–4 cannot produce a formula for them without extending accord_recipe_default. See 05_target_accords_without_recipes.csv.
4. Author the N=200 real (non-templated) prompts for the adversarial held-out set (plan F.5 / checkpoint L.7).

## P1 — Dictionary bijectivity checks (F.1)

- A dictionary is safe when every raw key maps to exactly one id (duplicate rows repeating the same mapping are harmless).
- `note_dictionary_master`: 899 rows, 0 ambiguous keys — **OK**
- `accord_dictionary_master`: 348 rows, 0 ambiguous keys — **OK**
- `descriptor_synonyms`: 96 rows, 0 ambiguous keys — **OK**
- `occasion_synonyms`: 50 rows, 0 ambiguous keys — **OK**
- output: [dictionary checks](./outputs/01_dictionary_checks.csv)
- `notes` catalog: 883 rows, 0 normalized names shared by multiple note_ids.

## P2 — Note-vocabulary bridge (F.1, checkpoint L.4)

- **rule→perfume**: 170/271 notes bridged (124 high, 46 medium/needs-review). Methods: {'unmatched': 101, 'exact_id': 50, 'stem_subset_single': 40, 'stem_equal': 36, 'cas': 34, 'fuzzy': 6, 'stem_subset_multi': 4}
- **perfume→rule**: 187/446 notes bridged (111 high, 76 medium/needs-review). Methods: {'unmatched': 259, 'stem_subset_single': 69, 'exact_id': 50, 'cas': 29, 'stem_equal': 27, 'fuzzy': 7, 'stem_subset_multi': 5}
- **Unified note map**: 883 catalog notes → 801 unified ids; 135 notes participate in merged groups (high-confidence matches only; medium pairs merge after human review).
- Blend weights, stems: FORM tokens stripped ['absolute', 'alcohol', 'butter', 'co2', 'coeur', 'concrete', 'eo', 'essence', 'extract', 'oil', 'otto', 'rectified', 'resinoid', 'tincture']; ORIGIN tokens stripped ['bourbon', 'bulgaria', 'calabria', 'china', 'egypt', 'france', 'haiti', 'india', 'indonesia', 'madagascar', 'morocco', 'mysore', 'siam', 'sicily', 'turkey', 'virginia']; single-token matches on ['accord', 'berry', 'blossom', 'flower', 'fruit', 'leaf', 'leaves', 'note', 'white', 'wood', 'woods'] are rejected as too generic.
- output: [bridge rule→perfume](./outputs/02_note_bridge_rule_to_perfume.csv)
- output: [bridge perfume→rule](./outputs/02_note_bridge_perfume_to_rule.csv)
- output: [unified note map](./outputs/02_unified_note_map.csv)
- 93 CAS numbers are shared by multiple catalog notes (251 rows). Cross-space shares drive the `cas` bridge tier; same-space shares are kept separate (often deliberate variants, e.g. oil vs absolute) and listed for review.
- output: [same-CAS note groups](./outputs/02_same_cas_note_groups.csv)

## P3 — Unmapped accord-token resolution (F.1)

- 10 unmapped tokens: 10 proposed accord mappings, 0 proposed descriptor-only, 0 need review.
- All 10 tokens matched an accord name **exactly** — the master workbook already added them to the `accords` sheet (ACC-0300…ACC-0309). The `unmapped_accord_tokens` sheet in notes_dataset_normalized.xlsx is stale relative to the master; treat the master catalog as authoritative.
- output: [token resolution proposals](./outputs/03_unmapped_accord_token_resolution.csv)

## P4 — Recipe cleanup (F.2)

- Dropped 196 exact-duplicate rows (1656 → 1460); duplicates were verbatim copied rows (ACC-0111 tripled, four accords doubled, 16 accords partially duplicated).
- Renormalised 102 accords back to a 100% sum (min/max scaled by the same factor, preserving window geometry).
- Post-clean invariants: sum-to-100 violations = 0; min ≤ default ≤ max violations = 0.
- Note-level table: 1400 (accord, note) rows across 235 accords with recipes.
- `accord_note` membership: dropped 87 exact-duplicate rows (1518 → 1431).
- Recipe/membership consistency: 0 accords have recipe notes missing from `accord_note` membership.
- output: [clean recipes (role level)](./outputs/04_recipe_clean_role_level.csv)
- output: [clean recipes (note level)](./outputs/04_recipe_clean_note_level.csv)
- output: [clean accord_note membership](./outputs/04_accord_note_clean.csv)

## P5 — Blended cross-accord recipes (F.2)

- Blend rule (a) from F.2 with placeholder weights **0.65/0.35** (primary/secondary) — replace with Stage-2 weights at inference; the lookup exists so training targets are precomputable.
- 2766/3048 unique (primary, secondary) pairs blended (30810 note rows); max |sum − 100| = 3.00e-06.
- Recipe coverage of training targets: 13 primary and 13 secondary target accords have **no recipe** in `accord_recipe_default`.
- output: [target accords without recipes](./outputs/05_target_accords_without_recipes.csv)
- output: [blended pair recipes](./outputs/05_blended_pair_recipes.csv)

## P6 — Note feature table (F.3)

- 883 notes × 8 numeric features; 3679 cells imputed via chemical_family means (per plan F.3) with is-imputed indicator columns; 446 notes had every numeric feature missing (fully family-imputed).
- Categoricals lower-cased with `unknown` fill: `chemical_family` (102 levels), `odor_family` (213 levels), `volatility_class` (8 levels), `solubility` (7 levels), `natural_source` (216 levels).
- **Deferred** (documented, per plan): CAS→SMILES→Morgan/Mordred/OpenPOM chemistry embeddings and MiniLM text embeddings for key_nuances / short_description. Both bolt onto this table by `note_id` without touching anything downstream.
- output: [note features](./outputs/06_note_features.csv)
- output: [categorical vocab](./outputs/06_note_categorical_vocab.json)

## P7 — Training-input features (F.4)

- Descriptor multi-hot: 98 vocab columns; synonym map applied; out-of-vocabulary tokens found: 0.
- Avoid multi-hot: 11 canonical tokens ['aldehydic', 'citrus', 'leather', 'musk', 'oud', 'patchouli', 'powdery', 'rose', 'smoky', 'sweet', 'vanilla'].
- Processed table: 10807 rows × 125 columns (raw text kept verbatim for the encoder; no leakage removal here — the templated-text risk is handled by the held-out real-prompt set, plan F.5).
- output: [processed training table](./outputs/07_training_processed.csv)
- output: [input vocab](./outputs/07_input_vocab.json)
- output: [Stage-1 feature spec](./outputs/07_feature_spec.json)

## P8 — Stratified splits and class weights (F.5)

- Seed 42; per-class allocation floor(10%) with a minimum of 1 example in val and test → sizes {'train': 8965, 'val': 921, 'test': 921}; 200/200 classes present in all three splits.
- Oversampling of rare classes is done at train time via the weight table below — never materialised into val/test (plan F.5).
- **Augmented data**: 807 synthetic rows (is_augmented=1, from perfume_system_master_training_dynamic_v5_augmented.xlsx) forced into the train split; val/test contain only original v4 rows, so metrics remain comparable with pre-augmentation runs.
- Class weights for the Stage-2 loss: inverse-frequency and class-balanced effective-number weights (Cui et al. 2019, β=0.999) over 200 training classes (head ACC counts 317–24).
- **Open item (plan F.5)**: the N=200 manually-written real prompts for the anti-template held-out set still need to be authored (checkpoint L.7).
- output: [splits](./outputs/08_splits.csv)
- output: [class weights](./outputs/08_class_weights.csv)
